# 🚀 Hamly.app Auto Push Tool

أداة الدفع التلقائي لمشروع Hamly.app - تدفع جميع التغييرات المحلية إلى فرع `main` على GitHub تلقائياً.

## 📋 المتطلبات

- Python 3.6+ مثبت على النظام
- Git مثبت ومُعد بشكل صحيح
- اتصال بالإنترنت للوصول إلى GitHub

## 🛠️ التثبيت

1. تأكد من وجود الملفات في جذر المشروع:
   - `auto_push.py` (السكريبت الرئيسي)
   - `auto_push.bat` (ملف Windows)

2. تأكد من أن Git مُعد بشكل صحيح:
   ```bash
   git config --global user.name "Your Name"
   git config --global user.email "your.email@example.com"
   ```

## 🚀 الاستخدام

### للويندوز (Windows)

#### الدفع التلقائي السريع:
```cmd
auto_push.bat
```

#### الوضع التفاعلي:
```cmd
auto_push.bat -i
```

#### عرض المساعدة:
```cmd
auto_push.bat --help
```

### لجميع الأنظمة (Python مباشرة)

#### الدفع التلقائي السريع:
```bash
python auto_push.py
```

#### الوضع التفاعلي:
```bash
python auto_push.py -i
```

#### رسالة مخصصة:
```bash
python auto_push.py "رسالة commit مخصصة"
```

#### عرض المساعدة:
```bash
python auto_push.py --help
```

## ⚙️ الميزات

- ✅ **دفع تلقائي**: يضيف جميع التغييرات ويعمل commit وpush تلقائياً
- ✅ **رسائل تلقائية**: رسائل commit مع الوقت والتاريخ
- ✅ **وضع تفاعلي**: تحكم كامل في العملية
- ✅ **تسجيل مفصل**: ملف log مع جميع العمليات
- ✅ **معالجة الأخطاء**: رسائل خطأ واضحة ومفيدة
- ✅ **دعم Unicode**: دعم كامل للعربية والإنجليزية

## 📝 أمثلة الاستخدام

### مثال 1: دفع سريع
```bash
python auto_push.py
```
النتيجة: دفع تلقائي مع رسالة "Auto push - 2024-01-15 14:30:25"

### مثال 2: دفع مع رسالة مخصصة
```bash
python auto_push.py "إضافة ميزة جديدة للمستخدمين"
```

### مثال 3: الوضع التفاعلي
```bash
python auto_push.py -i
```
سيعرض التغييرات الحالية ويطلب رسالة commit مخصصة

## 📊 ملفات السجل

- `auto_push.log`: ملف السجل المفصل لجميع العمليات
- يتم إنشاؤه تلقائياً في نفس مجلد المشروع

## 🔧 التخصيص

يمكنك تعديل المتغيرات في بداية ملف `auto_push.py`:

```python
class AutoPusher:
    def __init__(self):
        self.repo_path = os.getcwd()  # مسار المشروع
        self.branch = "main"          # اسم الفرع
        self.remote = "origin"       # اسم الـ remote
```

## 🚨 استكشاف الأخطاء

### خطأ: "Python غير مثبت"
- تثبيت Python من [python.org](https://python.org)
- التأكد من إضافة Python إلى PATH

### خطأ: "Git غير مثبت"
- تثبيت Git من [git-scm.com](https://git-scm.com)
- التأكد من إعداد Git بشكل صحيح

### خطأ: "Permission denied"
- التأكد من صلاحيات الكتابة في مجلد المشروع
- التحقق من إعدادات GitHub authentication

### خطأ: "Repository not found"
- التأكد من صحة رابط المستودع
- التحقق من صلاحيات الوصول إلى المستودع

## 📞 الدعم

إذا واجهت أي مشاكل، تحقق من:
1. ملف `auto_push.log` للتفاصيل
2. إعدادات Git والـ remote
3. اتصال الإنترنت
4. صلاحيات المستودع

## 🔄 التحديثات

لتحديث الأداة:
1. احذف الملفات القديمة
2. انسخ الملفات الجديدة
3. اختبر الأداة مرة أخرى

---

**ملاحظة**: هذه الأداة مصممة خصيصاً لمشروع Hamly.app وفرع `main`. تأكد من استخدامها في البيئة الصحيحة.
